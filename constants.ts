import { User } from './types';

// APP VERSION
export const APP_VERSION = '1.1.0';

// PRODUCTION CONFIGURATION
// This ensures the app always connects to your specific server
export const DEFAULT_API_URL = 'https://mr-v.ir/api.php'; 

// Local Storage Keys
export const LS_KEYS = {
  AUTH_TOKEN: 'nova_auth_token',
  USER_DATA: 'nova_user_data',
  THEME: 'nova_theme',
  LAST_CONTACT: 'nova_last_contact'
};