import React from 'react';

/**
 * ServerDocs component removed for security.
 * Please use the file provided in the deployment instructions.
 */
export const ServerDocs: React.FC<{ onBack: () => void }> = ({ onBack }) => {
  return null;
};